pre-commit:
  parallel: true
  jobs:
    - name: typecheck
      run: npx tsgo --noEmit
    - name: lint-format
      glob: "*.{ts,js,json,css,astro}"
      run: npx biome check --write {staged_files}
      stage_fixed: true
    - name: unused
      run: npx knip
