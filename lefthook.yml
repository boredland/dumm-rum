pre-commit:
  parallel: true
  jobs:
    - name: typecheck
      run: bunx tsc --noEmit
    - name: lint-format
      glob: "*.{ts,json}"
      run: bunx biome check --write {staged_files}
      stage_fixed: true
    - name: test
      run: bun test
