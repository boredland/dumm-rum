{
	"$schema": "https://schema.railpack.com",
	"steps": {
		"install": {
			"commands": ["bun install --frozen-lockfile --ignore-scripts"]
		},
		"build": {
			"commands": ["bun run build"]
		}
	},
	"deploy": {
		"startCommand": "bun serve.ts"
	}
}
